# Thème PILOTA90 — Landing Baseline (brouillon)

Fichiers déployés dans le thème Shopify **non publié**
« PILOTA90 — Landing Baseline (brouillon) » (id 197450072448).

| Fichier | Rôle |
|---|---|
| `layout/landing.liquid` | Layout autonome : tout le markup de la page |
| `templates/page.landing.liquid` | `{% layout 'landing' %}` — active le layout |
| `assets/p90-landing.css` | Styles (grille rem adaptative, composants) |
| `assets/p90-landing.js` | Module ES : ressorts, reveals, carrousels, loader |

Source lisible et non minifiée : `../pilota90.html`.
Ces fichiers en sont la version minifiée, découpée pour Shopify.

Page associée : « Landing Baseline (brouillon) », handle `landing-baseline`,
**non publiée**, template suffix `landing`.

Rien n'est en ligne : le thème est en brouillon et la page n'est pas publiée.
